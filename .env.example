# TinaCMS Cloud credentials (get these from https://app.tina.io after creating a project)
NEXT_PUBLIC_TINA_CLIENT_ID=
TINA_TOKEN=

# Optional: set the git branch (defaults to "main")
# GITHUB_BRANCH=main
